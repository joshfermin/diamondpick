{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "0.17.1",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.0"
  },
  "scripts": {
    "start": "PORT=8080 react-scripts start",
    "build": "PORT=8080 react-scripts build",
    "test": "PORT=8080 react-scripts test --env=jsdom",
    "eject": "PORT=8080 react-scripts eject"
  },
  "proxy": "http://localhost:8081"
}
